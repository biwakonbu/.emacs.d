;;-- interface settings --;;

;; スクロールバー
(require 'yascroll)
(global-yascroll-bar-mode 1)
(scroll-bar-mode 0)

;; スムーススクロール
(require 'smooth-scroll)
(smooth-scroll-mode t)

;; カーソルの位置を真ん中あたりに固定する
(require 'smooth-scrolling)
